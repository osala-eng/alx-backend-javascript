/**
 * A function that accepts two numbers and returns the sum of the two numbers.
 * @param {number} a  Number a
 * @param {number} b Number b
 * @returns Sum of a and b
 */
module.exports = (a, b) => Math.round(a) + Math.round(b);
